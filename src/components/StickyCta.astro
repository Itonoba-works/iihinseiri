---
import site from '../data/site.json';
import companies from '../data/companies.json';
const {
  text = '認定業者を一括で比較',
  small = '提携サイトへ移動します',
  label = site.estimateLabel,
  affId = site.estimateAffId
} = Astro.props;
const c = affId ? companies.find((x) => x.id === affId) : null;
const external = c && c.affUrl ? c.affUrl : '';
let href = '/rankings/ihin.html';
if (c) href = external || (c.hasReview ? '/reviews/' + c.id + '.html' : '/rankings/' + c.category + '.html#' + c.id);
---
<div class="sticky-cta" id="stickyCta">
  <div class="sticky-cta-inner">
    <div class="sticky-cta-text">
      {text}
      {small && <small>{small}</small>}
    </div>
    <a
      href={href}
      class="btn-cta pulse"
      data-aff={c ? c.id : undefined}
      data-aff-external={external ? true : undefined}
      data-aff-pos="sticky"
      rel={external ? 'sponsored nofollow noopener' : undefined}
      target={external ? '_blank' : undefined}
    >{external && <span class="pr">PR</span>}{label}<span class="arrow">→</span></a>
  </div>
</div>
